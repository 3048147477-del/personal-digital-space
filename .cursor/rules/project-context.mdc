---
description: 个人数字空间的长期项目上下文与同步维护规则
globs:
alwaysApply: true
---

# 项目上下文规则

@project-context.md

- 根目录 `project-context.md` 是本项目唯一完整事实源；每次规划、设计或编码前先读取它。
- 当前项目是面向所有感兴趣者的公开个人数字空间，核心内容是个人经历、游戏、书籍、音乐和电影。
- 首版没有真实产品，不创建空产品页；不以 AI 培训、企业咨询或服务销售为主要目标。
- 已确认采用职责清晰的多页结构、React + Vite + TypeScript、本地结构化数据、移动端和明暗主题支持；首版路由使用项目内 History API 轻量实现。
- 视觉方向为 `Quiet Reveal / 安静显影`：暖白、炭黑、单一薄荷青与一次深色反转。
- 首版使用克制 L2 交互，以 CSS + IntersectionObserver 做逐层显影和章节状态变化；不使用重型动效依赖。
- `DESIGN.md` 已由用户确认；V1 基础版已经实现并通过本地构建与浏览器验证，后续代码仍须严格遵循。
- V1 已完成首次公开 GitHub 存档；部署与正式上线仍待处理。
- 当前所有个人资料与收藏内容都是明确的待补状态；加入真实数据前不得编造姓名、经历、封面、时长、评价或联系方式。
- 当用户确认的定位、受众、页面、路由、技术、设计、内容模型、素材状态或项目阶段发生变化时，在同一次修改中更新 `project-context.md` 及其决策记录。
- 不要将猜测、临时调试信息、密钥、Token 或个人敏感信息写入长期上下文。
