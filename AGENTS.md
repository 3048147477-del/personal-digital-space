# 项目协作规范

## 协作原则

- 永远先 plan，后 coding。
- 重大修改前先解释原因、改动文件、可能影响。
- 一次只处理一个明确目标。
- 不要无意义新增依赖。
- 保持代码简洁、可复用、易读。
- 默认支持移动端与暗黑模式。
- 文案避免 AI 套话，要具体、自然。

## 项目长期上下文

- 每次 plan 或 coding 前先读取根目录的 `project-context.md`；它是当前快照和上下文路由，不再包含全部历史。
- 根据 `project-context.md` 的“按任务读取”表，只加载当前目标需要的分类文档；实际导入收藏时读取 `CONTENT_SYNC.md`，不要默认读取全部 `PRODUCT.md`、`DESIGN.md` 和 `docs/context/`。
- 当已确认的定位、页面、技术、设计、内容模型或项目阶段发生变化时，在同一次修改中更新对应分类文档；只有当前快照变化时才同步更新 `project-context.md`。
- 历史原因只追加到 `docs/context/decisions.md`，不要重新堆回根上下文。
- 不要把猜测、临时调试信息、密钥、Cookie、Token 或个人敏感数据写入长期上下文。

## 视觉参考与实现硬规则

以下规则来自 2026-08-12 首页返工，属于强制约束，不得以“先做个效果看看”为由跳过。

### 先观察，再翻译

- 用户给出参考站时，编码前必须实际观察每个可访问网站的硬刷新瞬间、进入中段、稳定首屏和至少一个滚动或点击状态；记录标题、主体、留白的实际矩形与占比。
- 观察结论必须分成三类写清楚：`原站可见事实`、`对事实的判断`、`本站的原创翻译`。不得把自己的翻译说成原站做法。
- 参考对象必须按语义角色翻译，而不是按视觉位置替换。吊牌、产品卡、品牌字形、作品图和人物头像不是同一种主体；原站右侧有“物件”不等于本站应放头像。
- 用户要求参考“全部体验”时，检查范围必须覆盖加载、进入、滚动、过渡、导航、按钮、微组件、媒体、排版、内容密度和页尾，不能只抽取身份卡或单个版式。
- 每个机制选择一个主参考并说明为什么适配产品；其他参考只能补充，禁止把四站显眼元素拼成一张页面。

### 首屏与中文标题

- 桌面首屏在 `scrollY = 0`、滚动中段、标题完成、反向滚动、点击推进五个状态都必须有明确设计稿式定义，不能只设计最终画面。
- 未滚动时，中文文字只能完整显示一个可读语义单位，或完整隐藏。禁止横向裁切产生“慢慢认”一类半词、残句或半个汉字。
- 滚动显影优先按整行或完整短语组织，并使用纵向遮罩、空间位移或层级交接；不得为了“渐入”破坏中文阅读。
- 未渐入的首屏不能只是空白，也不能随意拿头像或装饰物件填空；应展示与产品角色一致的一个完整锚点。本项目当前只使用居中的完整昵称与轻量滚动提示，除共享导航外不再叠加眉题、索引封面、边栏或人物事实。
- 头像是身份档案中的次级信息，不是默认 Hero 主视觉。首页头像只能在完整身份 Folio 进入后于卡片内部出现，除非用户以后明确改变此规则。
- 中文展示字必须用真实文案检查字号、字重、行距、字距和换行；不能只看 CSS 数值。相邻两行不得挤压，标题也不得依靠过重字形独占整个首屏。
- 不监听或改写 `wheel`，不强制翻页；滚动和点击可以抵达同一状态，但必须保留原生滚动控制。

### 视觉问题诊断

- 用户反馈“单调、没层次、颜色不舒服”时，先判断问题属于结构层级、材料关系、信息密度还是色彩角色；不得先用渐变、叠纸、阴影或新增颜色遮盖结构问题。
- 首页一次只允许一个主视觉重心。新增主体前必须说明原有主体如何退出或降级，禁止标题、头像、身份卡和按钮同时争夺注意力。
- 被用户否定的概念必须从代码、设计规范、当前快照和状态文档中一并移除；不能只把它淡化后继续保留。

### 桌面验收与陈述

- 当前移动端开发、截图和验收完全暂停，直到用户明确恢复；“默认支持移动端”不构成主动修改移动端的授权。
- 当前视觉修改至少检查用户正在预览的桌面尺寸和 `1440×1000`；移动端暂停期间不得把移动检查混入交付。
- 所有占比、位置和是否重叠的结论以渲染后的 `getBoundingClientRect()` 或截图为准，不能把网格列数当成实际占比汇报。
- 宣布完成前必须检查：加载结束后的初始态、滚动中段、稳定完成态、反向收回、点击与键盘路径、横向溢出、控件重叠、控制台错误、类型检查和生产构建。
- 验证过程中看到的过渡帧必须等待动画停稳后再判断；同时要确认过渡帧本身没有残句、闪烁或错误主体。

### 本轮错误的直接禁令

- 禁止再次把“参考站的单一物件”直接翻译成首页大头像。
- 禁止再次在未滚动时显示被裁断的中文标题。
- 禁止在没有逐站证据矩阵时声称“参考了四个网站”。
- 禁止使用未经渲染测量的百分比描述最终构图。
- 禁止在用户暂停移动端后继续移动端开发、截图或专项验收。
